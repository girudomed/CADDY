# Caddyfile

# ── QR-BOT ───────────────────────────────────────────────────────
qr.girumed.ru {
    # health-check
    @health path /health
    respond @health 200

    # прокси на ваше приложение
    reverse_proxy qrbot:8080

    # сжатие
    encode zstd gzip
}

# ── n8n ─────────────────────────────────────────────────────────
n8n.portalgm.ru {
    # прокси на n8n
    reverse_proxy n8n_n8n_1:5678

    # сжатие
    encode zstd gzip
}