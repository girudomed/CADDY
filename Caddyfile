{
    # Только HTTP-01, не пытаться через ALPN/tls-alpn-01
    servers {
        automatic_https {
            disable_tls_alpn_challenge
        }
    }
}

# ── QR-BOT ───────────────────────────────────────────────────────
qr.girumed.ru {
    @health path /health
    respond @health 200

    reverse_proxy qrbot:8080
    encode zstd gzip
}

# ── n8n ─────────────────────────────────────────────────────────
n8n.portalgm.ru {
    reverse_proxy n8n_n8n_1:5678
    encode zstd gzip
}